version: "3"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.production
    image: eternalexpansion/devops-for-developers-project-74
    working_dir: /app
    command: make start
    ports:
      - "8080:8080"
